<%= form_with(model: skater, local: true) do |form| %>
  <% if skater.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(skater.errors.count, "error") %> prohibited this skater from being saved:</h2>

      <ul>
      <% skater.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name, id: :skater_first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name, id: :skater_last_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
